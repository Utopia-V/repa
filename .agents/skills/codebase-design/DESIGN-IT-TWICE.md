# 比较不同 interface

当至少两种实质不同的 interface 都可能满足同一项已确认职责，而且选择会改变调用方负担、非法状态或总系统复杂度时使用。

固定共同的可观察结果与现实约束，让模型自由推导责任划分和 seam 不同的候选。独立视角确有信息价值时可以委派，但不规定候选或 Agent 数量。

用相同的正常场景、失败场景和一次可能变化理解各候选，比较调用方必须知道什么、状态与恢复由谁拥有、变化是否集中，以及整个系统增加多少机制。给出一个有判断的推荐；没有候选实质改善结果时保留现状。
