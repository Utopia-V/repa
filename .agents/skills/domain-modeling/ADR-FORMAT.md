# ADR 格式

ADR 位于 `docs/adr/`，使用 `NNNN-slug.md` 顺序编号。目录在第一条真实决定出现时创建。

```markdown
# <决定标题>

<用一至三段说明当时必须决定什么、最终决定、决定性原因，以及必要的适用范围或失效条件。>
```

只有状态变化、非显然的后果或值得保留的替代方案确实会帮助未来维护时才增加对应小节。ADR 记录当前有效决定，不保存讨论时间线，也不复制可以从代码、Git 或上游契约直接恢复的事实。
