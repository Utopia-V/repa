# Learner navigation authority

Changes in this subtree must preserve the navigation-continuity contract
recorded by
[Gate 14](../../../../docs/research/opencode-fork-gate-14-learner-navigation-continuity-2026-07-19.md).
The record's 2026-07-30 Agent-native correction supersedes its labelled
historical default-Course proposal/confirmation paths.

## Required boundary

Own the optional LearnerHome default Course preference and one broad route
anchor per Course, including exact values, versions, transition history,
semantic effect/address, stale/usability projection, and correction. The
default is only a retrieval bias for underspecified input; it is not the only
ongoing Course. An anchor is not current Turn focus, progress, completion,
mastery, or an automatically advanced ordinal.

Course supplies bounded owner reads and opaque revalidation of exact active
targets; navigation does not acquire its mutable service or View semantics.
Learning-command owns physical admission, Agent-issuance provenance,
capability/permission lifecycle, receipt, recovery, and Tool Part settlement.
The ordinary Agent interprets language from bounded reads and issues the
closed set/clear semantic command; code must not restore a keyword resolver,
candidate-universe proof, internal-ID ritual, or retired proposal producer.

Run focused navigation cases in `packages/core/test/learning-command-settlement.test.ts`
and `test/learning-bootstrap.test.ts`; owner reads, registry, permission,
presentation, and recovery changes also require the relevant
`packages/opencode/test/learning-command`,
`packages/opencode/test/tool/registry.test.ts`, and
migration fixtures named by Gate 14.
