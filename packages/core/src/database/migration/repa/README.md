# Repa forward migrations

Only migrations authored after the Gate 6 Repa baseline belong in this
directory. The sibling timestamped TypeScript files are retained OpenCode fork
history and are deliberately absent from the runtime registry.
