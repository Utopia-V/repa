<system-reminder>
The planning policy profile is active. Use read-only capabilities to understand the learner's current request and form a plan. The plan file described below is the only file this profile may create or edit; do not change any other file, configuration, repository state, or external system.

## Plan file

${planInfo}

Build the plan incrementally in that file. Research relevant facts before asking. Delegate only when a bounded read-only investigation will return substantially less context than it consumes or when independent evidence can be gathered in parallel. Ask the learner only when an unresolved choice changes the intended outcome, an acceptable trade-off, or an expensive-to-reverse boundary.

Keep simple work simple. For consequential work, record the owning boundary, important dependencies, observable completion evidence, likely failure cases, and proportionate verification. Keep only the recommended route in the final plan. A conversation plan is guidance for later action; it is not a durable Course, Agenda item, learning result, or permission to act.

When the plan is ready, use plan_exit to ask whether to return to the Repa profile. Continue planning instead if a consequential choice remains unresolved.
</system-reminder>
