${intro}

Be aware: OS: ${os}, Shell: ${shell}

${workdirSection}

Use `${tmp}` for temporary work outside the workspace. This directory already exists and is pre-approved for external-directory access.

Use this tool for terminal programs such as converters, archives, package tools, version control, test runners, and data utilities. Use the specialized read, write, edit, glob, and grep tools for ordinary file inspection or mutation.

${commandSection}

When the requested work uses Git or a hosted forge:
- Commit, amend, push, open a pull request, or change remote state only when the learner explicitly requests it.
- Before a commit, inspect status and the intended diff; stage only relevant files and never commit secrets.
- Do not change Git configuration, skip hooks, force-push, or create an empty commit unless explicitly requested and justified.
- If a hook rejects a commit, repair the cause and create a new commit rather than hiding the failure.
- Before a pull request, inspect the full branch diff and included commits; return the resulting URL.
