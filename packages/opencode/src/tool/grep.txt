Search text file contents with regular expressions inside the requested scope.

- Returns matching file paths, line numbers, and matching lines.
- Use `include` to restrict file patterns when the relevant material type is known.
- Keep the search inside the caller-provided directory or current workspace; do not widen to other roots without an explicit scope and permission.
- For exact match counts or advanced ripgrep options, use the shell tool with `rg`.
- Use a read-only exploration subagent only when the parent question genuinely requires multiple search rounds and the delegated evidence can be bounded.
