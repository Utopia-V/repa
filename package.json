{
  "name": "agentic-learning-system",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "repa": "bun run src/cli.ts",
    "check:reference": "bun run scripts/check-reference.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test --isolate",
    "check": "bun run check:reference && bun run typecheck && bun run test"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "2.0.41",
    "ai": "6.0.168",
    "zod": "4.1.8"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
